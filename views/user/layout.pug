doctype html
html
    head
        include ../head

        style .top-bar {border-color: #1a237e !important; box-shadow: 0 3px 6px rgba(0,0,0,0.16),0 3px 6px rgba(0,0,0,0.23) !important;}
    body
        include top-bar

        div(class='bulletin_list')

        div(class='content')
            block content

script(type="module") import { ui } from '/javascripts/ui/ui.mjs'; $(document).ready(ui.init);